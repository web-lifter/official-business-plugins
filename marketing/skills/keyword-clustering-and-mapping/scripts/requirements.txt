# keyword-clustering-and-mapping — self-contained engine dependencies.
# Core (always required):
pandas>=2.0
scikit-learn>=1.3
numpy>=1.26
nltk>=3.8
plotly>=5.18
markdown>=3.5

# Semantic embeddings (mpnet/mini/e5/bge presets). Pulls torch — large first
# install. The runner degrades to tf-idf automatically if this is absent.

# Advanced clustering (hdbscan auto-count, UMAP projection, ANN graph).

# Optional static image export of Plotly charts (kept <1.0 to avoid needing a
# system Chrome install). Not required for the interactive HTML dashboard.

# Sanitise rendered reports before embedding in the offline dashboard.
bleach>=6,<7
