# Generated by scripts/sync-openai.py; edit the skill metadata instead.
interface:
  display_name: Brand Identity
  short_description: Run the brand identity workflow
  default_prompt: Use $brand-identity with the supplied context and evidence.
